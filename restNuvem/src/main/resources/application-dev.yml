spring:
  datasource:
    url: jdbc:h2:mem:santanderdio2025
    username: champs
    password:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create #validate, update, create-drop, create
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
        trace: false